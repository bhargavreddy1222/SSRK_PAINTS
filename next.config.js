/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: ['http://localhost:3000', 'https://shop-web-phi.vercel.app','192.168.0.5'],
}

module.exports = nextConfig
